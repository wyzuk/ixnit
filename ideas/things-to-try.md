# Things to test & learn

- [ ] CSS `subgrid` (how well supported is it now?)
- [ ] Web Workers for heavy array math
- [ ] Server-Sent Events (SSE) vs WebSockets for simple live feeds
- [ ] Node `node:test` built-in test runner (so I don't need Jest/Vitest for small scripts)
