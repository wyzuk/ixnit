# Random thoughts & questions

- Why does every UI library need 500 dependencies when native HTML `<dialog>`, `<details>`, and CSS `grid` exist now?
- Need to look into SQLite in the browser with WASM (sql.js or official sqlite wasm).
- Would be cool to build a tiny single-file wiki in vanilla JS.
